#include <bits/stdc++.h>

using namespace std;

#define repi(i, j, k)for(int i=j;i<k;++i)
int v[100005];

int main()
{
        ios::sync_with_stdio(false);
        int t;
        cin >> t;
        while (t--) {
                int s, c, r;
                repi(i, 0, s) {
                        cin >> v[i];
                }
        }
        return 0;
}
