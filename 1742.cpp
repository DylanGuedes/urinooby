#include <iostream>
#include <algorithm>
#include <cstring>

int arr[1005][1005];

using namespace std;

int main()
{
  int n, m, n_buracos;
  while(cin >> n >> m) {
    memset(arr, 0, sizeof arr);
    cin >> n_buracos;
  }


  return 0;
}
